📩 Промт для Replit-агента — «Сделать меню и кнопки в боте»

Кратко: реализуй полнофункциональное меню в Telegram-боте (aiogram), содержащее все кнопки и диалоги согласно ТЗ. Интерфейс должен работать с уже существующими функциями/базой — не менять бизнес-логику (создание ссылок, сохранение событий и т.п.). Пиши чистый, тестируемый код, добавь обработчики на inline-кнопки и conversation flow для вводимых данных.

🔧 Технические требования

Стек: Python 3.10+, aiogram v3.x (использовать async), python-dotenv для config.

Не трогать существующие модули, которые реализуют: create_invite_for(name), get_invites(), export_excel(), add_user_manual(data), delete_user(username), find_user(username), get_stats(period) и т.п. Если таких функций нет — создай адаптеры/заглушки, которые вызывают соответствующие DB/бизнес-функции.

Все команды/кнопки, требующие прав — проверять is_admin(user_id) (использовать существующую проверку).

Логи/ошибки — логировать через logging.

Callback data: короткие префиксы menu:..., action:.... В коде использовать CallbackData/безопасное парсирование.

🧭 Меню (полная карта)
Главное меню (Admin)

InlineKeyboard с кнопками (один ряд = одна строка):

➕ Создать пригласительную ссылку → открывает flow создания ссылки

👥 Список пригласительных → показывает пост с перечнем ссылок + кнопки управления каждой ссылки

📊 Отчёты → открывает вложенное меню

🏆 Рейтинг → показывает топ пригласителей (кнопка обновления)

🔎 Найти пользователя → open input flow (ввод @username или id)

📝 Добавить пользователя вручную → open form flow (ввод user_id, username, name, inviter)

❌ Удалить пользователя → flow (ввод username/id)

🔔 Настройки уведомлений → opens settings menu (время отчётов, получатели)

📤 Экспорт Excel → вызывает export_excel() и шлёт файл

Кнопка «Экспорт Excel» доступна только для админов.

Подменю — Отчёты

📅 Сегодня — получить краткий отчёт за сегодня (сообщение)

📊 Неделя — отчёт за 7 дней

📈 Месяц — отчёт за 30 дней

📒 Полный Excel-отчёт — экспорт всего файла (шлёт файл)

Меню для пригласителя (обычный пользователь)

(доступно только в личке бота)

📥 Мои приглашённые — список приглашённых пользователем с их статусами

🏆 Моя статистика — краткая сводка (всего / удержание / %)

📤 Скачать Excel (мои данные) — формирует и шлёт Excel только с данными этого пригласителя (если это разрешено)

🔁 Потоки ввода (conversation flows)

Все flows — реализовать через FSM (aiogram FSMContext) или простые state-handler-ы.

Создать пригласительную ссылку

Нажали ➕ Создать пригласительную ссылку → бот спрашивает:

Выберите существующего пригласителя или напишите новое имя:

Показать клавиатуру с существующими именами (get_inviter_list()) + кнопка ➕ Новый пригласитель

Если выбран существующий — вызов create_invite_for(name) → вернуть ссылку и показать кнопку Скопировать / Отправить.

Если новый — бот спрашивает Напишите имя → создаёт.

Список пригласительных

Показать пост вида:

Вадим:
 - https://t.me/xxx?start=abc123  [Удалить] [Параметры]
 - https://t.me/xxx?start=def456  [Удалить] [Параметры]
Анель:
 - ...


Каждая ссылка — inline кнопки Удалить, Деактивировать, Инфо (инфо показывает статистику по этой ссылке).

Найти пользователя

Нажали 🔎 Найти пользователя → бот просит ввести @username или tg_user_id.

После ввода — вызывает find_user() и выводит историю (входы/выходы), статус удержания, пригласитель(и).

Кнопки в ответе: 🗂 Показать все записи в Excel (выгрузить файл с фильтром).

Добавить пользователя вручную

Ввод последовательный: tg_user_id, username, name, приливитель (имя или ссылка), дата входа (опционально).

Вызвать add_user_manual(data) и подтвердить: Пользователь добавлен.

Удалить пользователя

Спрашивать подтверждение: Вы точно хотите удалить @username? Да / Нет.

При Да → delete_user(username) и ответ Удалено.

Настройки уведомлений

Показать текущие значения: время отправки, список получателей.

Кнопки Изменить время (flow ввода HH:MM), Добавить получателя (ввод user_id), Удалить получателя.

Сохранение через set_report_time() и set_admins_list() (адаптеры к .env/db).

🧩 CallbackData и названия

Используй структуру callback data, например:

menu:create_invite

menu:list_invites

menu:reports:today / menu:reports:week / menu:reports:month / menu:reports:excel

menu:rating

menu:find_user

action:invite:delete:<invite_id>

action:invite:info:<invite_id>

Ограничение: callback не должен быть >64 байт — при необходимости передавай только id и парси.

✅ Важные правила интеграции (не менять логику)

Не трогать существующую бизнес-логику: создание ссылок, сохранение событий, проверку удержания. Интерфейс должен только вызывать существующие функции/адаптеры.

Проверки прав: все admin-функции — только для is_admin(user_id); если не админ — показывать дружелюбное сообщение.

Идемпотентность: если action уже выполнен (например, ссылка уже удалена) — корректно информировать пользователя, а не падать.

📦 Файловая структура (что добавить)

Добавить/обновить модули:

/handlers/ui.py         # строит inline keyboards и message handlers
/handlers/flows.py      # FSM flows (create invite / find user / manual add / delete)
/utils/adapter.py       # thin wrappers: create_invite_for(), get_invites(), export_excel(), find_user()...
/bot.py                 # подключает новые хэндлеры; не менять остальной бот-логики

🧪 Тесты и acceptance criteria

Добавить тесты (pytest):

Нажатие 📊 Отчёты → Сегодня возвращает корректный текст (мокать get_stats).

Flow Создать пригласительную ссылку:

при выборе существующего имени — вызывает create_invite_for(name) и отправляет ссылку.

при создании нового — вызывает создание и отвечает.

🔎 Найти пользователя — ввод @user возвращает строку с историей (мок DB).

Кнопка 📤 Экспорт Excel — вызывает export_excel() и бот отправляет файл (мок send_document).

Неправильный пользователь (не админ) — не видит админских кнопок/получает ошибку.

Acceptance:

UI полностью соответствует карте меню.

Все элементы работают через inline-кнопки (без необходимости вводить команды вручную, кроме логики ввода данных).

Admin only: только админы видят и могут использовать админские действия.

Нет изменений в базовой логике работы бота.

📣 Deliverables (что сдавать агенту)

PR/коммит с добавлением новых модулей handlers/ui.py, handlers/flows.py, utils/adapter.py.

README с инструкцией: как запускать, как тестировать flows.

Unit tests (pytest) для основных flows.

Демонстрация: короткое видео/GIF или лог с прогона flows: создание ссылки → просмотр списка → поиск пользователя → экспорт Excel.

✍️ Пример кода (шаблон inline-keyboard, aiogram)

(вставлять как пример — агент должен написать аккуратно!)

from aiogram.types import InlineKeyboardButton, InlineKeyboardMarkup

def main_admin_kb():
    kb = InlineKeyboardMarkup(row_width=2)
    kb.add(
      InlineKeyboardButton("➕ Создать пригласительную ссылку", callback_data="menu:create_invite"),
      InlineKeyboardButton("👥 Список пригласительных", callback_data="menu:list_invites"),
      InlineKeyboardButton("📊 Отчёты", callback_data="menu:reports"),
      InlineKeyboardButton("🏆 Рейтинг", callback_data="menu:rating"),
      InlineKeyboardButton("🔎 Найти пользователя", callback_data="menu:find_user"),
      InlineKeyboardButton("📝 Добавить вручную", callback_data="menu:manual_add"),
      InlineKeyboardButton("❌ Удалить пользователя", callback_data="menu:delete_user"),
      InlineKeyboardButton("🔔 Настройки", callback_data="menu:settings"),
      InlineKeyboardButton("📤 Экспорт Excel", callback_data="menu:export_excel")
    )
    return kb


Если что-то неясно — агенту можно задавать уточняющие вопросы, но перед началом реализации должен быть подтверждён список существующих адаптерных функций (create_invite_for, get_invites и т.д.).